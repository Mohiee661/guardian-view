services:
  - type: web
    name: guardian-view-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir backend app:app
    envVars:
      - key: GROQ_API_KEY
        sync: false
